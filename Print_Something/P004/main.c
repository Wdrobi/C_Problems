/*Write a C program to multiply two numbers (4 and 5) and
display its product.*/
#include <stdio.h>

int main()
{
    int number1 = 4, number2 = 5;

    // print the numbers and their multiplication
    printf("The production = %d", number1 * number2);

    return 0;
}